$( document ).ready( function() {

	checkSize();
    $( window ).resize( checkSize );

});

function checkSize() {
    
    if ( $( ".mobile" ).css( "display" ) == "block" ) {
    
    }

    if ( $( ".tablet-portrait" ).css( "display" ) == "block" ) {
        
    }

    if ( $( ".tablet-landscape" ).css( "display" ) == "block" ) {
        
    }

    if ( $( ".desktop" ).css( "display" ) == "block" ) {
        
    }

    /*
    if ( $( "body" ).css( "width" ) >= "1280px" ) {

    }
    */
}